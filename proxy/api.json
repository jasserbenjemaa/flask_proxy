  {
    "type": "object",
    "properties": {
        "received data from the consumer": {
            "type": "object"
        },
        "message": {
            "type": "string"
        },
        "source": {
            "type": "string"
        }
    },
    "required": ["received data from the consumer", "message", "source"]
}
